# This is an example of your .env file format, which pnpm db:setup will create.
# Note: this must be .env, not .env.local, without further configuration changes.
POSTGRES_URL=postgresql://***
STRIPE_SECRET_KEY=sk_test_***
STRIPE_WEBHOOK_SECRET=whsec_***
BASE_URL=http://localhost:3000
AUTH_SECRET=***

# Resend (required for real email sending; fallback logs locally if empty)
RESEND_API_KEY=re_***
EMAIL_FROM="Karriereadler <noreply@karriereadler.com>"

# Stripe Product Prices (configure in Stripe Dashboard)
STRIPE_PRICE_CV_SINGLE=price_***
STRIPE_PRICE_LETTER_SINGLE=price_***
STRIPE_PRICE_BUNDLE=price_***

# Vercel Blob Storage (required for PDF uploads in production)
# Get from: https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_***

# Optional: forward contact form submissions
# CONTACT_FORWARD_EMAIL=support@karriereadler.com
# ADMIN_NOTIFY_EMAIL=info@karriereadler.com
